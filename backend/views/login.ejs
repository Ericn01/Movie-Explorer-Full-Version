<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>User Login Page</title>
        <style>
            body{
                background-color: #fafafa;
                font-family: Arial, Helvetica, sans-serif;
                background-image: linear-gradient(navy, violet);
                height: 100vh;
            }
            h1{
                text-align: center;
                margin-left: 15px;
            }
            .form-wrapper{
                width: 100%;
                height: 95%;
                display: flex;
                justify-content: center;
                align-items: center;
            }
            form{
                background-color: #fafafa;
                border-radius: 10px;
                width: 35%;
                max-width: 350px;
                height: max-content;
                padding: 10px 25px;
                box-shadow: 1px 1px 5px darkgray;
            }
            .input-container{
                font-size: 14.5px;
                margin: 35px 5px;
                display: flex;
                justify-content: space-between;
            }
            input{
                outline: none;
                border: none;
                border-bottom: 2px solid lightgray;
            }
                input:focus{
                border-bottom: 2px solid blue;
            }
                .required-symbol{
                color: red;
                font-weight: bold;
            }
            .button{
                justify-content: center;
            }
            button{
                margin-top: 10px;
                padding: 8.5px 0px;
                color: white;
                background-color: #677acf;
                border-radius: 15px;
                border: none;
                width: 85%;
                font-weight: bold;
            }
            button:hover{
                transition: 0.5s ease-in;
                background-color: #4759a6;
                cursor: pointer;
            }
            .top-container{
                display: flex;
                align-items: center;
                margin-bottom: 10px;
                border-bottom: 1px solid rgba(0, 0, 0, 0.1)
            }
            .small-text{
                color: gray;
                font-size: 12px;
                font-style: italic;
            }
                .optional{
                justify-content: center;
                margin: 10px 0px;
            }
            .guest{
                text-decoration: underline;
                cursor: pointer;
                font-style:normal
            }
            .guest:hover{
                color: blue;
            }
            .message-container{
                padding: 10px;
                margin: 10px 0px;
                font-size: 13px;
                border-radius: 5px;
            }
            .error{
                background-color: lightyellow;
            }
            .success {
                background-color: lightgreen;
            }
        </style>
    </head>
    <body> 
        <section class="form-wrapper">
            <form method="post" action="/users/login">
                <div class="top-container">
                    <img src="https://cdn-icons-png.flaticon.com/512/152/152532.png" alt="login" width="28px" height="28px" draggable="false">
                    <h1> Log In </h1>
                </div>
                <% if(typeof loginErrors != 'undefined'){ %>
                    <div class="message-container error">
                        <% loginErrors.forEach((err) => { %>
                            <%= err.message %>
                        <% }); %>
                    </div>
                <% } %>
                <% if(auth_error != ''){ %>
                    <div class="message-container error">
                        <%= auth_error %>
                    </div>
                <% } %>
                <% if(auth_success != ''){ %>
                    <div class="message-container success">
                        <%= auth_success %>
                    </div>
                <% } %>
                <div class="input-container email">
                    <label for="email">Email<span class="required-symbol">*</span>: </label>
                    <input type="text" id="email" name="email" value="al@ace.ca" placeholder="Enter Email" required>
                </div>
                <div class="input-container password">
                    <label for="password">Password<span class="required-symbol">*</span>: </label>
                    <input type="password" id="password" name="password" value="mypassword" placeholder="Enter Password" required>
                </div>
                <div class="input-container button">
                    <button> Login </button>
                </div>
            </form>
        </section>
    </body>
</html>
